
```
in-dir-exec X.Y.Z

USAGE:
    in-dir-exec <cwd> <cmd> [arg]...

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

ARGS:
    <cwd>       cmd workdir
    <cmd>       cmd to exec
    <arg>...    cmd args
```